/* Design system adapted from matthewyu.dev stylesheet (tokens + layout).
 * Typeface: Jost as the open Futura stand-in — we do not hotlink his font files. */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
	:root {
		--paper: #e6e9ed;
		--ink: #1d1f22;
		--ink-soft: rgba(29, 31, 34, 0.55);
		--hairline: rgba(29, 31, 34, 0.14);
		--ph: #d7dbe0;
		--accent: #3d6ced;
		--font: 'Jost', 'Futura', 'Avenir Next', system-ui, sans-serif;
		--track-caps: 0.24em;
		--track-nav: 0.12em;
		--track-display: 0.05em;

		/* Kept for Tailwind / markdown utilities that still reference HSL tokens. */
		--background: 210 14% 91%;
		--foreground: 220 7% 12%;
		--card: 210 20% 96%;
		--card-foreground: 220 7% 12%;
		--popover: 210 20% 96%;
		--popover-foreground: 220 7% 12%;
		--primary: 220 7% 12%;
		--primary-foreground: 210 14% 91%;
		--secondary: 210 16% 94%;
		--secondary-foreground: 220 7% 12%;
		--muted: 210 16% 94%;
		--muted-foreground: 220 6% 40%;
		--accent-hsl: 221 80% 58%;
		--accent-foreground: 210 14% 91%;
		--destructive: 0 72% 45%;
		--destructive-foreground: 0 0% 100%;
		--border: 210 12% 84%;
		--input: 210 12% 84%;
		--ring: 220 7% 12%;
		--radius: 0.25rem;
		--surface: #f4f6f8;
	}

	.dark {
		--paper: #060608;
		--ink: #f2f4f7;
		--ink-soft: rgba(242, 244, 247, 0.6);
		--hairline: rgba(242, 244, 247, 0.18);
		--ph: #1a1b1f;
		--accent: #6b8ef5;
		--surface: #121316;

		--background: 240 7% 2%;
		--foreground: 210 20% 96%;
		--card: 240 6% 7%;
		--card-foreground: 210 20% 96%;
		--popover: 240 6% 7%;
		--popover-foreground: 210 20% 96%;
		--primary: 210 20% 96%;
		--primary-foreground: 240 7% 2%;
		--secondary: 240 5% 12%;
		--secondary-foreground: 210 20% 96%;
		--muted: 240 5% 12%;
		--muted-foreground: 210 10% 65%;
		--accent-hsl: 221 80% 68%;
		--accent-foreground: 240 7% 2%;
		--destructive: 0 62% 45%;
		--destructive-foreground: 210 20% 96%;
		--border: 240 5% 16%;
		--input: 240 5% 16%;
		--ring: 210 20% 96%;
	}

	* {
		box-sizing: border-box;
	}

	html {
		scroll-behavior: smooth;
		scroll-padding-top: 96px;
		overflow-x: clip;
		-webkit-text-size-adjust: 100%;
	}

	body,
	html {
		overflow-x: clip;
	}

	body {
		font-family: var(--font);
		font-weight: 400;
		background: var(--paper);
		color: var(--ink);
		-webkit-font-smoothing: antialiased;
		transition: background-color 0.55s ease;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	img {
		display: block;
		max-width: 100%;
		height: auto;
	}

	::selection {
		background: rgba(29, 29, 31, 0.85);
		color: var(--paper);
	}

	.dark ::selection {
		background: rgba(242, 244, 247, 0.88);
		color: #060608;
	}

	h1,
	h2,
	h3,
	h4,
	h5,
	h6 {
		color: var(--ink);
		text-wrap: balance;
		font-weight: 400;
	}

	:focus-visible {
		outline: 1.5px solid color-mix(in srgb, var(--ink) 40%, transparent);
		outline-offset: 3px;
		border-radius: 2px;
	}
}

@layer components {
	/* —— Page shell —— */
	.page {
		min-height: 100dvh;
		padding: calc(clamp(28px, 5vw, 72px) + 56px) clamp(28px, 5vw, 72px) 10vh;
		max-width: 980px;
		margin: 0 auto;
		animation: page-fade 0.45s ease both;
	}

	.page.home {
		padding-bottom: clamp(28px, 5vh, 56px);
	}

	.page.home .foot {
		margin-top: clamp(40px, 8vh, 90px);
	}

	section {
		scroll-margin-top: 96px;
		margin-bottom: clamp(64px, 11vh, 140px);
	}

	.section-label {
		font-size: 11px;
		letter-spacing: var(--track-caps);
		text-transform: uppercase;
		color: var(--ink-soft);
		margin-bottom: 20px;
	}

	/* —— Top bar —— */
	.top {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		z-index: 500;
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding-inline: clamp(20px, 4vw, 48px);
		padding-block: 16px 72px;
		pointer-events: none;
	}

	.top > * {
		pointer-events: auto;
	}

	.top::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: -1;
		background: linear-gradient(
			180deg,
			rgba(230, 233, 237, 0.97),
			rgba(230, 233, 237, 0.9) 45%,
			rgba(230, 233, 237, 0)
		);
		-webkit-backdrop-filter: blur(16px) saturate(0.9);
		backdrop-filter: blur(16px) saturate(0.9);
		-webkit-mask-image: linear-gradient(180deg, #000 42%, transparent);
		mask-image: linear-gradient(180deg, #000 42%, transparent);
	}

	.dark .top::before {
		background: linear-gradient(
			180deg,
			rgba(6, 6, 8, 0.97),
			rgba(6, 6, 8, 0.9) 45%,
			rgba(6, 6, 8, 0)
		);
		-webkit-backdrop-filter: blur(16px) saturate(0.9);
		backdrop-filter: blur(16px) saturate(0.9);
	}

	.top .name {
		font-size: 15px;
		font-weight: 300;
		white-space: nowrap;
		flex: none;
		letter-spacing: var(--track-nav);
		color: var(--ink);
	}

	.top nav {
		display: flex;
		gap: 22px;
		font-size: 13px;
		align-items: center;
		letter-spacing: var(--track-nav);
	}

	.top nav a {
		color: var(--ink-soft);
		transition: color 0.2s;
		text-transform: lowercase;
	}

	.top nav a:hover,
	.top nav a[aria-current='page'] {
		color: var(--ink);
	}

	.top nav .top-socials {
		display: flex;
		align-items: center;
		gap: 14px;
		margin-left: 12px;
		padding-left: 20px;
		border-left: 1px solid var(--hairline);
	}

	.icon-btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 4px;
		color: var(--ink-soft);
		transition: color 0.2s;
		background: transparent;
		border: 0;
		cursor: pointer;
	}

	.icon-btn:hover {
		color: var(--ink);
	}

	/* —— Links —— */
	.link,
	.bio-link {
		font-weight: 500;
		color: var(--ink);
		text-decoration: underline;
		text-decoration-color: rgba(29, 29, 31, 0.25);
		text-underline-offset: 3px;
		transition: text-decoration-color 0.2s;
	}

	.dark .link,
	.dark .bio-link {
		text-decoration-color: rgba(242, 244, 247, 0.28);
	}

	.link:hover,
	.bio-link:hover {
		text-decoration-color: var(--ink);
	}

	.link-quiet {
		color: var(--ink-soft);
		transition: color 0.2s;
	}

	.link-quiet:hover {
		color: var(--ink);
	}

	.rule {
		border: 0;
		border-top: 1px solid var(--hairline);
	}

	.back {
		display: inline-block;
		font-size: 13px;
		letter-spacing: var(--track-nav);
		color: var(--ink-soft);
		transition: color 0.2s;
		margin-bottom: clamp(24px, 4vh, 40px);
	}

	.back:hover {
		color: var(--ink);
	}

	/* —— Hero —— */
	.hero {
		position: relative;
		display: grid;
		justify-items: center;
		align-content: center;
		margin-top: calc(-1 * (clamp(28px, 5vw, 72px) + 56px));
		min-height: 100svh;
		padding-bottom: clamp(48px, 7svh, 66px);
		overflow-anchor: none;
		margin-bottom: 0;
	}

	.hero-kicker {
		font-size: 11.5px;
		font-weight: 300;
		letter-spacing: var(--track-caps);
		text-transform: uppercase;
		text-align: center;
		color: var(--ink-soft);
		margin-bottom: clamp(18px, 3vh, 34px);
	}

	.hero-name {
		position: relative;
		z-index: 0;
		font-weight: 500;
		font-size: clamp(56px, 11vw, 118px);
		letter-spacing: var(--track-display);
		color: var(--accent);
		filter: blur(6px);
		text-align: center;
		line-height: 1;
		white-space: nowrap;
		margin-bottom: -0.52em;
		user-select: none;
		pointer-events: none;
	}

	.hero-stage {
		position: relative;
		z-index: 1;
		display: grid;
		justify-items: center;
		gap: 20px;
		width: min(100%, 420px);
	}

	.hero-portrait {
		position: relative;
		width: min(68vw, 300px);
		display: grid;
		justify-items: center;
		gap: 20px;
	}

	/* Soft under-glow — same trick as .sb-book:before */
	.hero-portrait::before {
		content: '';
		position: absolute;
		left: 10%;
		right: 10%;
		top: 8%;
		bottom: 16%;
		border-radius: 18px;
		background: rgba(20, 22, 30, 0.18);
		filter: blur(28px);
		z-index: -1;
	}

	.hero-portrait img {
		width: 100%;
		border-radius: 4px;
		box-shadow: 0 18px 48px rgba(20, 22, 30, 0.22);
		aspect-ratio: 3 / 4;
		object-fit: cover;
		object-position: center 18%;
	}

	.hero-caption {
		font-size: 12px;
		letter-spacing: var(--track-caps);
		text-transform: uppercase;
		color: var(--ink-soft);
	}

	.hero-down {
		position: absolute;
		bottom: 44px;
		left: 50%;
		transform: translateX(-50%);
		border: 0;
		background: transparent;
		padding: 10px 14px;
		color: var(--ink);
		cursor: pointer;
		-webkit-tap-highlight-color: transparent;
		animation: cue-breathe 2.6s ease-in-out infinite;
	}

	.hero-down:hover {
		animation: none;
		opacity: 1;
	}

	/* —— About band —— */
	.about {
		display: grid;
		grid-template-columns: auto auto;
		justify-content: center;
		align-items: center;
		gap: clamp(24px, 4vw, 44px);
		margin-top: clamp(16px, 4vh, 48px);
	}

	.about-text .bio {
		max-width: 480px;
		font-size: 15px;
		line-height: 1.95;
		letter-spacing: 0.025em;
		color: rgba(29, 31, 34, 0.85);
		text-align: left;
	}

	.dark .about-text .bio {
		color: rgba(242, 244, 247, 0.82);
	}

	.about .headshot {
		width: clamp(96px, 14vw, 132px);
		flex: none;
	}

	.about .headshot img {
		width: 100%;
		height: auto;
		border-radius: 4px;
	}

	/* —— Project / writing rows —— */
	.rows {
		border-top: 1px solid var(--hairline);
	}

	.row {
		position: relative;
		display: grid;
		grid-template-columns: 3.2em 1fr auto;
		align-items: baseline;
		gap: 16px;
		padding: 17px 2px;
		border-bottom: 1px solid var(--hairline);
		transition: opacity 0.15s;
		color: inherit;
	}

	.row-thumb {
		position: absolute;
		right: 7.5em;
		top: 50%;
		width: 116px;
		aspect-ratio: 3 / 2;
		transform: translateY(-50%) scale(0.9);
		border-radius: 3px;
		overflow: hidden;
		background: var(--ph);
		box-shadow: 0 12px 34px rgba(20, 22, 30, 0.24);
		opacity: 0;
		pointer-events: none;
		transition: opacity 0.22s ease, transform 0.22s ease;
		z-index: 6;
	}

	.row-thumb img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	/* Text preview when a post has no image — glass card instead of empty ph. */
	.row-thumb.is-text {
		width: min(220px, 42vw);
		aspect-ratio: auto;
		padding: 12px 14px;
		background: rgba(238, 241, 244, 0.72);
		border: 1px solid var(--hairline);
		-webkit-backdrop-filter: blur(14px);
		backdrop-filter: blur(14px);
	}

	.dark .row-thumb.is-text {
		background: rgba(18, 18, 22, 0.72);
	}

	.row-thumb .preview-title {
		font-size: 13px;
		letter-spacing: 0.02em;
		margin-bottom: 6px;
		color: var(--ink);
	}

	.row-thumb .preview-body {
		font-size: 12px;
		line-height: 1.55;
		color: var(--ink-soft);
		display: -webkit-box;
		-webkit-line-clamp: 3;
		line-clamp: 3;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.row:hover .row-thumb {
		opacity: 1;
		transform: translateY(-50%) scale(1);
	}

	.row .year {
		font-size: 12px;
		color: var(--ink-soft);
		letter-spacing: var(--track-nav);
	}

	.row .title {
		font-size: clamp(17px, 2vw, 21px);
		letter-spacing: 0.03em;
	}

	.row .blurb {
		font-size: 13px;
		color: var(--ink-soft);
		letter-spacing: 0.03em;
		text-align: right;
		text-transform: lowercase;
	}

	.rows:hover .row:not(:hover) {
		opacity: 0.45;
	}

	/* —— Project / article headers —— */
	.proj-head {
		margin-bottom: clamp(32px, 6vh, 64px);
	}

	.proj-head h1 {
		font-weight: 300;
		font-size: clamp(30px, 5vw, 54px);
		letter-spacing: var(--track-display);
	}

	.proj-head .meta {
		margin-top: 10px;
		font-size: 13px;
		color: var(--ink-soft);
		letter-spacing: var(--track-nav);
	}

	.proj-head .lede,
	.proj-body {
		max-width: 620px;
		display: grid;
		gap: 1.1em;
		font-size: 15.5px;
		line-height: 1.75;
		letter-spacing: 0.02em;
		color: rgba(29, 29, 31, 0.82);
	}

	.dark .proj-head .lede,
	.dark .proj-body {
		color: rgba(242, 244, 247, 0.82);
	}

	.proj-head .lede {
		margin-top: 14px;
	}

	.proj-body {
		margin-bottom: clamp(40px, 7vh, 80px);
	}

	.pager {
		display: flex;
		justify-content: space-between;
		gap: 24px;
		margin-top: clamp(48px, 8vh, 96px);
		padding-top: 20px;
		border-top: 1px solid var(--hairline);
		font-size: 13px;
		letter-spacing: var(--track-nav);
		color: var(--ink-soft);
	}

	.pager a:hover {
		color: var(--ink);
	}

	/* —— Studio-style grid —— */
	.play-list {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
		gap: clamp(14px, 2vw, 26px);
	}

	.play-list + .section-label {
		margin-top: clamp(64px, 11vh, 140px);
	}

	.play-card .thumb {
		position: relative;
		aspect-ratio: 16 / 10;
		overflow: hidden;
		border-radius: 3px;
		background: var(--ph);
	}

	.play-card .thumb img {
		transition: transform 0.5s ease;
	}

	.play-card:hover .thumb img {
		transform: scale(1.04);
	}

	.play-card .thumb-fallback {
		display: grid;
		place-items: center;
		height: 100%;
		font-size: clamp(28px, 4vw, 40px);
		font-weight: 300;
		letter-spacing: var(--track-display);
		color: var(--ink-soft);
		background:
			radial-gradient(
				120% 80% at 20% 0%,
				color-mix(in srgb, var(--accent) 18%, transparent),
				transparent 55%
			),
			var(--ph);
	}

	.play-card .cap {
		display: flex;
		justify-content: space-between;
		align-items: baseline;
		margin-top: 11px;
		gap: 12px;
	}

	.play-card .cap .t {
		font-size: 16px;
		letter-spacing: 0.03em;
	}

	.play-card .cap .n {
		font-size: 12px;
		color: var(--ink-soft);
		letter-spacing: 0.02em;
		text-align: right;
	}

	.foot {
		font-size: 11px;
		letter-spacing: var(--track-caps);
		text-transform: uppercase;
		color: var(--ink-soft);
		text-align: center;
		padding-top: 4vh;
	}
}

@layer utilities {
	.no-highlight {
		-webkit-tap-highlight-color: transparent;
		-webkit-touch-callout: none;
	}

	.tnum {
		font-variant-numeric: tabular-nums;
	}

	.track-caps {
		letter-spacing: var(--track-caps);
	}

	.track-nav {
		letter-spacing: var(--track-nav);
	}

	.text-ink-70 {
		color: rgba(29, 31, 34, 0.85);
	}

	.dark .text-ink-70 {
		color: rgba(242, 244, 247, 0.82);
	}

	.text-ink-50 {
		color: var(--ink-soft);
	}

	.text-ink-30 {
		color: color-mix(in srgb, var(--ink) 32%, transparent);
	}
}

@keyframes page-fade {
	0% {
		opacity: 0;
	}
}

@keyframes cue-breathe {
	0%,
	100% {
		opacity: 0.15;
	}
	50% {
		opacity: 0.75;
	}
}

@media (max-width: 640px) {
	.top {
		padding-inline: 12px;
		padding-block: 12px 56px;
	}

	.top .name {
		font-size: 12px;
	}

	.top nav {
		gap: 10px;
		font-size: 11px;
		letter-spacing: 0.07em;
	}

	.top nav .top-socials {
		gap: 8px;
		margin-left: 0;
		padding-left: 10px;
	}

	.top .icon-btn {
		padding: 2px;
	}

	.row {
		grid-template-columns: 3em 1fr;
	}

	.row .blurb,
	.row-thumb {
		display: none;
	}

	.hero-name {
		white-space: normal;
		font-size: clamp(42px, 14vw, 72px);
	}
}

@media (max-width: 560px) {
	.about {
		grid-template-columns: 1fr;
		gap: 22px;
	}

	.about .headshot {
		order: -1;
	}
}

@media (max-width: 600px) {
	.row-thumb {
		display: none;
	}
}

@media (prefers-reduced-motion: reduce) {
	html {
		scroll-behavior: auto;
	}

	.page,
	.hero-down,
	.row,
	.row-thumb {
		animation: none !important;
		transition: none !important;
	}

	.hero-down {
		opacity: 0.5;
	}

	.hero-name {
		filter: blur(4px);
	}
}
